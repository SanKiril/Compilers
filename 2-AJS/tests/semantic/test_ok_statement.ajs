// SIMPLE DEFINITIONS
type Object = {attr1: int};

// COMPLEX DEFINITIONS
type Obj = {attr1: Object, "attr2": boolean};

// SINGLE DECLARATIONS
let a;

// MULTIPLE DECLARATIONS
let b, c, d;

// SINGLE TYPED DECLARATIONS
let e: Object;

// MULTIPLE TYPED DECLARATIONS
let f: Obj, g: Obj, h;

// EXPRESSION ASSIGNMENTS
a = 1;

// EXPRESSION REASSIGNMENTS
a = 12.34;
a = tr;
a = null;
a = 6 * 10 / 2;

// OBJECT ASSIGNMENTS
f = {attr1: {attr1: 12}, attr2: tr};
g = null;
g = f;

// OBJECT ATTRIBUTE ASSIGNEMENTS
f.attr2 = 2 < 4;
a = fl;
f.attr2 = a;
f.attr1 = {attr1: 3 + -2};
e = {attr1: ''' + ''' -1};
f.attr1 = e;

// SINGLE DECLARATIONS & ASSIGNMENTS
let i = 1;

// MULTIPLE DECLARATIONS & ASSIGNMENTS
let j, k, l = 0xfF;
let m, n, o = null;

// MULTI-OBJECT ASSIGNMENTS
let p: Object, q: Object, r: Object = {attr1: 12};
