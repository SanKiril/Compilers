// SIMPLE DEFINITIONS
type Object = {attr1: int};

// COMPLEX DEFINITIONS
type Obj = {attr1: Object, "attr2": boolean};

// SINGLE DECLARATIONS
let a;

// MULTIPLE DECLARATIONS
let b, c, d;

// SINGLE TYPED DECLARATIONS
let e: Obj;

// MULTIPLE TYPED DECLARATIONS
let f: Object, g: Object, h;

// EXPRESSION ASSIGNMENT
a = 1;

// EXPRESSION REASSIGNMENTS
a = 12.34;
a = tr;
a = null;
a = 6 * 10 / 2;

// OBJECT ASSIGNMENT
f = {attr1: 12};
g = null;
g = f;

// SINGLE DECLARATION & ASSIGNMENT
let i = 1;

// MULTIPLE DECLARATIONS & ASSIGNMENTS
let j, k, l = 0xfF;

// MULTI-OBJECT ASSIGNMENT
let m: Object, n: Object, o: Object = {attr1: 12};
